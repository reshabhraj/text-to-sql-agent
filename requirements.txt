# The guardrails depend on sqlglot's AST classes; a new major may rename them.
sqlglot>=30,<31
python-dotenv>=1.1
requests>=2.32
pytest>=8
# psycopg[binary]>=3.2  (optional, only for DATABASE_URL pointing at PostgreSQL)
